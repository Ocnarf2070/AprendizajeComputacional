a1<- c(0, 10,2, 6,4,1,8,10,6)
b1 <-c(250,150,90,78,20,170,160,180,200)
c1<-c(36,34,10,8,1,70,41,38,45)
d1<-c("H", "M","H", "M", "M", "H",  "M",  "H", "H")
simpsons<- data.frame(a1, b1, c1, d1)
names(simpsons)<- c("Pelo", "Peso", "Edad", "Genero")
a1<- c(8 ,9)
b1 <-c(29,135)
c1<-c(38.32)
d1<-c("H","M")
simpsonsTest<- data.frame(a1, b1, c1, d1)
names(simpsonsTest)<- c("Pelo", "Peso", "Edad", "Genero")
require(party)
arbolSimpsons<- ctree(Genero~., data=simpsons)
plot(arbolSimpsons)
#text(arbolSimpsons, use.n=TRUE)
print(arbolSimpsons)
table(predict(arbolSimpsons,simpsons), simpsons$Genero)
#table(predict(arbolSimpsons,  simpsons , type = "response"))
lr.pred <- predict(arbolSimpsons,simpsonsTest,type="response")
lr.pred
testPred <- predict(arbolSimpsons, newdata = simpsonsTest)
table(lr.pred, simpsonsTest$Genero)
lr.pred2 <- predict(arbolSimpsons,simpsons,type="response")
table(lr.pred2, simpsons$Genero)
plot(arbolSimpsons)
print(arbolSimpsons)
